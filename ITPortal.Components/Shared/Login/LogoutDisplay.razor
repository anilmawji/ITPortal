@using Microsoft.AspNetCore.Components.Authorization
@using System.Security.Claims

@inject AuthenticationStateProvider AuthenticationStateProvider

<AuthorizeView>
    <Authorized>
        <p class="welcome">Welcome, @context.User.FindFirst(ClaimTypes.Email)?.Value!</p>
        <button class="logout-btn" @onclick=@Logout>Log out</button>
    </Authorized>
</AuthorizeView>

@code {
    private void Logout()
    {
        ((ExternalAuthStateProvider)AuthenticationStateProvider).Logout();
    }
}
